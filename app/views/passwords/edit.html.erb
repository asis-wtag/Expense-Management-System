<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
  <div class="text-center mb-5">
    <h1><%= I18n.t('view.password.update_heading_text') %></h1>
  </div>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow">
        <div class="card-body">
          <%= form_with model: current_user, url: password_path do |form| %>
            <% if form.object.errors.any? %>
              <div class="alert alert-danger">
                <% form.object.errors.full_messages.each do |message| %>
                  <div><%= message %></div>
                <% end %>
              </div>
            <% end %>
            <div class="mb-3">
              <%= form.label :password_challenge, I18n.t('view.password.current_password_label_text'), class: "form-label" %>
              <%= form.password_field :password_challenge, class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= form.label :password, I18n.t('view.password.new_password_label_text'), class: "form-label" %>
              <%= form.password_field :password, class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= form.label :password_confirmation, I18n.t('view.password.confirm_password_label_text'), class: "form-label" %>
              <%= form.password_field :password_confirmation, class: "form-control" %>
            </div>
            <div>
              <%= form.submit I18n.t('view.password.button_text'), class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
</body>
</html>
